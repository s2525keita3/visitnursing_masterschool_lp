# AIハンドオフ・ドキュメント

**プロジェクト名:** kigyo-lp (訪問看護ステーション開業支援LP)  
**最終更新日:** 2024年  
**目的:** 他の生成AI（ChatGPT, Claude, Gemini等）が一発で理解し、開発を継続できる包括的な技術ドキュメント

---

## 📋 目次

1. [プロジェクト概要](#1-プロジェクト概要-project-identity)
2. [ディレクトリ構造](#2-ディレクトリ構造-file-tree)
3. [デザインシステム](#3-デザインシステム-design-dna)
4. [データ構造とコンテンツ](#4-データ構造とコンテンツ-data-schema)
5. [コンポーネントアーキテクチャ](#5-コンポーネントアーキテクチャ-component-map)
6. [運用・開発ルール](#6-運用開発ルール-development-rules)

---

## 1. プロジェクト概要 (Project Identity)

### プロジェクト名
`kigyo-lp` - 訪問看護ステーション開業支援スクールのランディングページ

### 目的
訪問看護ステーションの開業準備者・経営者向けに、88万円の受講費を「最高の投資」として提示し、無料セミナーへの申し込み（CV）を最大化するLP

### 技術スタック

| カテゴリ | 技術 | バージョン |
|---------|------|-----------|
| **Framework** | React | 19.2.0 |
| **Language** | TypeScript | ES2020 |
| **Styling** | Tailwind CSS | 4.1.17 |
| **Bundler** | Vite | 7.2.4 |
| **Icons** | Lucide React | 0.554.0 |
| **Linting** | ESLint | 9.39.1 |

### ビルド設定
- **開発サーバー:** `npm run dev` (ポート5173)
- **本番ビルド:** `npm run build`
- **チャンク分割:** vendor (react/react-dom), icons (lucide-react)
- **ミニファイ:** esbuild

---

## 2. ディレクトリ構造 (File Tree)

```
src/
├── App.tsx                    # メインアプリケーション（全セクションの統合）
├── main.tsx                   # エントリーポイント
├── index.css                  # グローバルスタイル・Tailwind設定
├── vite-env.d.ts             # Vite環境変数の型定義
│
├── content/                   # 📦 データ層（コンテンツ・文言の一元管理）
│   ├── achievements.ts        # 実績データ（現在未使用）
│   ├── benefits.ts            # 5つのベネフィット + 受講生の成果データ
│   ├── founder.ts             # 講師情報・プログラムフェーズ
│   ├── hero.ts                # Heroセクションの文言・背景画像パス
│   ├── legal.ts               # 特定商取引法・プライバシーポリシー
│   ├── pricing.ts             # 価格情報・パッケージ内容
│   ├── problems.ts            # 問題提起セクションのデータ
│   ├── program.ts             # カリキュラム詳細（現在未使用）
│   ├── roi.ts                 # ROI（投資対効果）データ
│   ├── seminar.ts             # セミナー情報・日程
│   └── testimonials.ts        # 受講生の声（Before/After含む）
│
├── features/                  # 🎨 機能層（セクション別コンポーネント）
│   ├── hero/
│   │   └── Hero.tsx           # ファーストビュー（通常ロード）
│   ├── problems/
│   │   └── Problems.tsx       # 問題提起セクション
│   ├── benefits/
│   │   └── Benefits.tsx       # 5つのベネフィット + 受講生の成果
│   ├── testimonials/
│   │   └── Testimonials.tsx   # 受講生の声（Before/After表示）
│   ├── founder/
│   │   └── Founder.tsx        # 講師紹介 + プログラム内容
│   ├── roi/
│   │   └── RoiSection.tsx     # 投資対効果セクション
│   ├── pricing/
│   │   └── PricingSection.tsx # 価格提示セクション
│   ├── seminar/
│   │   └── Seminar.tsx         # 無料セミナー案内
│   ├── contact/
│   │   └── ContactForm.tsx    # お問い合わせフォーム（Formspree連携）
│   └── program/
│       └── ProgramHighlights.tsx # プログラム詳細（現在未使用）
│
└── shared/                    # 🔧 共通層（再利用可能なコンポーネント・ユーティリティ）
    ├── components/            # 共通コンポーネント
    │   ├── CtaSection.tsx     # CTAセクション（緑背景・特典画像）
    │   ├── ErrorBoundary.tsx  # エラーハンドリング
    │   └── Metadata.tsx       # SEOメタデータ管理
    │
    ├── constants/             # 定数定義
    │   ├── animations.ts      # アニメーション遅延定数
    │   ├── styles.ts          # 背景画像パス
    │   └── ui.ts              # UI定数（スクロール閾値等）
    │
    ├── hooks/                 # カスタムフック
    │   ├── useAnimationDelays.ts    # Heroアニメーション遅延
    │   ├── useForm.ts               # フォーム状態管理
    │   ├── useIntersectionObserver.ts # スクロール検知
    │   ├── useScrollListener.ts     # スクロールイベント
    │   ├── useStaggeredAnimation.ts # 段階的アニメーション
    │   └── useThrottle.ts           # パフォーマンス最適化
    │
    ├── layout/                # レイアウトコンポーネント
    │   ├── Footer.tsx         # フッター
    │   ├── Header.tsx         # ヘッダー
    │   ├── ProgressBar.tsx    # スクロール進捗バー
    │   └── StickyBottomCTA.tsx # 固定CTAボタン（画面下部）
    │
    ├── types/                 # 型定義
    │   └── index.ts           # 共通型（ContactFormData等）
    │
    ├── ui/                    # UIコンポーネント
    │   ├── Card.tsx           # カードコンポーネント
    │   ├── CTAButton.tsx      # CTAボタン（3サイズ対応）
    │   ├── ImageWithFallback.tsx # 画像フォールバック
    │   ├── Pill.tsx           # バッジ風ラベル
    │   ├── Section.tsx        # セクションラッパー
    │   └── SectionHeading.tsx  # セクション見出し
    │
    └── utils/                 # ユーティリティ関数
        ├── cn.ts              # className結合（clsx風）
        ├── metadata.ts        # メタデータ生成
        └── validation.ts      # フォームバリデーション
```

### アーキテクチャの原則

1. **データとロジックの分離**
   - `content/`: すべての文言・データを一元管理
   - `features/`: データを読み込んで表示するロジックのみ

2. **コード分割（Lazy Loading）**
   - `Hero`のみ通常ロード（ファーストビュー）
   - その他すべて`lazy()` + `Suspense`で遅延読み込み

3. **DRY原則**
   - 共通コンポーネントは`shared/`に集約
   - アニメーション遅延は定数化（`constants/animations.ts`）

---

## 3. デザインシステム (Design DNA)

### カラーパレット

#### ブランドカラー（オレンジ系）
```css
--color-baum-50: #FFF8F0   /* 背景クリーム */
--color-baum-100: #FFF5EB
--color-baum-200: #FFE6CC
--color-baum-300: #FFD6AD
--color-baum-400: #FFB570
--color-baum-500: #FF6B00   /* メインオレンジ（brand-orange） */
--color-baum-550: #FF8533
--color-baum-600: #E55A00
--color-baum-700: #CC5000
--color-baum-800: #A33F00
--color-baum-900: #6B2A00
```

#### Tailwindクラス名
- `brand-orange`: `#FF6B00`
- `brand-dark`: `#000000`（テキスト）
- `brand-muted`: `#666666`（サブテキスト）
- `brand-light`: `#FFF8F0`（背景）

#### テキストカラー
```css
--color-text-primary: #000000
--color-text-secondary: #333333
--color-text-tertiary: #666666
--color-text-light: #999999
```

#### 背景カラー
```css
--color-bg-cream: #FFF8F0
--color-bg-orange-light: #FFF5EB
```

### フォント

- **ファミリー:** `"Noto Sans JP"`（日本語最適化）
- **フォールバック:** `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto`
- **読み込み戦略:** `font-display: swap`（FOUT防止）
- **見出し:** `font-weight: 700`（`font-black`相当）

### UIルール

#### 角丸（Border Radius）
- **カード:** `rounded-3xl` (24px)
- **ボタン:** `rounded-full`（完全な円形）
- **小要素:** `rounded-2xl` (16px)

#### シャドウ（Shadow）
```css
--shadow-card: 0 8px 32px rgba(0, 0, 0, 0.12)
--shadow-card-hover: 0 16px 48px rgba(0, 0, 0, 0.16)
--shadow-cta: 0 12px 32px rgba(255, 107, 0, 0.35)
--shadow-cta-hover: 0 16px 40px rgba(255, 107, 0, 0.45)
```

#### タッチターゲットサイズ（アクセシビリティ）
```typescript
TOUCH_TARGETS = {
  MINIMUM: 44,      // WCAG推奨最小値
  RECOMMENDED: 48,  // 推奨値
  LARGE: 56,        // 大きなボタン
}
```

### 主要コンポーネントスタイル

#### CTAButton
- **サイズ:** `small` (44px), `default` (48px), `large` (56px)
- **スタイル:** グラデーション背景（`gradient-cta`）、シャドウ付き
- **アニメーション:** `animate-shake-slow`（2秒間隔でプルプル）

#### Section
- **パディング:** `px-4 py-20 sm:px-6 sm:py-24 md:px-8 md:py-32 lg:py-40`
- **トーン:** `plain` (白), `muted` (グラデーション1), `surface` (グラデーション2)

#### Card
- **スタイル:** `card-elevated`クラス
- **ホバー:** `hover-lift`（8px上に浮く）

### アニメーション

#### キーフレーム
- `fadeUp`: 下からフェードイン
- `fadeIn`: 単純フェードイン
- `shakeSlow`: ゆっくり揺れる（CTAボタン用）
- `barGrow`: バーが伸びる（ROIセクション用）

#### アニメーション遅延定数
```typescript
ANIMATION_DELAYS = {
  BASE: 0.1,
  STAGGER: 0.1,
  HERO_TARGET: 0.1,
  HERO_TITLE: 0.2,
  HERO_OFFER: 0.25,
  HERO_CTA: 0.4,
}
```

---

## 4. データ構造とコンテンツ (Data Schema)

### Testimonial型（受講生の声）

```typescript
type Testimonial = {
  id: number;
  image: string;              // WebP画像パス（例: "/eme-houkan-daihyo.webp"）
  name: string;              // 受講生名（例: "北山 様"）
  role: string;              // 役職・所属（例: "えめ訪問看護ステーション和歌山 代表"）
  title: string;             // 見出し（改行含む）
  content: string;           // 感想文（互換性のため残す）
  before?: string;           // Before状態の説明
  after?: string[];         // After状態の箇条書き
  link?: string;            // 公式サイトURL
  reverse: boolean;         // レイアウト（画像左右の配置）
};
```

**データ例:**
```typescript
{
  id: 1,
  image: "/eme-houkan-daihyo.webp",
  name: "北山 様",
  role: "えめ訪問看護ステーション和歌山 代表",
  title: "「土地勘ゼロの地で、まさかの融資2,000万円超。\n『準備不足』の私が、開業3ヶ月で黒字化できた理由」",
  before: "何を準備すればいいか分からず、不安だけが残っていた状態。",
  after: [
    "想定の2倍、2,000万円超の融資を獲得",
    "開業3ヶ月で単月黒字化を達成",
    "8ヶ月目で月間500件超、毎月10件の新規依頼",
    "6ヶ月で2拠点目の準備に入れるほどキャッシュが安定",
  ],
  reverse: false,
}
```

### StudentResult型（受講生の成果）

```typescript
type StudentResult = {
  name: string;
  location: string;
  headline: string;
  videoPoster: string;        // WebP画像パス
  videoUrl: string;          // YouTube URL
  website: string;            // 公式サイトURL
  before: string;
  after: string[];
  testimonial: string;
  contextTag: string;        // カード上部のタグ（例: "経営未経験から開業"）
  overlayHighlights: string[]; // 動画サムネイル上に表示する成果ハイライト
  quantitativeResults: {
    metrics: string;         // 例: "月商 2.3倍 / 採用2名"
    description: string;
  };
};
```

### Benefit型（5つのベネフィット）

```typescript
type Benefit = {
  image: string;             // WebP画像パス（例: "/benefit1.webp"）
  title: string;             // 短縮されたタイトル
  description: string;       // 説明文
};
```

### PricingData型（価格情報）

```typescript
type PricingFeature = {
  id: string;
  text: string;
};

const pricingData = {
  mainTitle: "受講費用",
  subTitle: "フランチャイズのような「月額ロイヤリティ」は一切不要...",
  badge: "第1期生 限定価格",
  originalPrice: "1,100,000円",
  currentPrice: "880,000円",
  priceNote: "（税込）",
  royaltyText: "月額ロイヤリティ：永年0円",
  features: PricingFeature[],
  paymentNote: "※分割払い対応（無料説明会にてご相談ください）",
};
```

### LegalData型（法的データ）

```typescript
const legalData = {
  tokushoho: [
    { label: "販売業者", value: "一般社団法人 全国訪問看護経営研究協会" },
    { label: "運営統括責任者", value: "代表理事 渋谷 慶太" },
    // ... その他の項目
  ],
  privacy: string; // HTML文字列（プライバシーポリシー）
};
```

### ContactFormData型（フォームデータ）

```typescript
type ContactType = "seminar" | "consultation";

type ContactFormData = {
  name: string;
  email: string;
  phone: string;
  message: string;
  contactType: ContactType;
  seminarDate: string;
};
```

---

## 5. コンポーネントアーキテクチャ (Component Map)

### App.tsx（メインアプリケーション）

**セクション順序（CRO最適化）:**
1. `Hero` - ファーストビュー（通常ロード）
2. `CtaSection` - 即決用CTA
3. `Problems` - 共感セクション
4. `Benefits` - 解決策（動画あり）
5. `Testimonials` - 証拠（Before/After）
6. `CtaSection` - 中間CTA
7. `Founder` - 権威・信頼
8. `RoiSection` - 投資対効果
9. `PricingSection` - 価格提示
10. `Seminar` - リスクリバーサル
11. `ContactForm` - アクション
12. `CtaSection` - 最終クロージング

**依存関係:**
- すべてのセクションは`lazy()`で読み込み（Hero除く）
- `Suspense`でローディング状態を管理
- `ErrorBoundary`でエラーハンドリング

### 主要コンポーネントの依存関係

#### Hero.tsx
```
Hero
├── ImageWithFallback (背景画像)
├── CTAButton
├── useHeroAnimationDelays (アニメーション)
└── heroContent (content/hero.ts)
```

#### Benefits.tsx
```
Benefits
├── Section
├── CTAButton
├── ImageWithFallback
├── useStaggeredAnimation
├── benefits (content/benefits.ts)
└── studentResults (content/benefits.ts)
```

#### Testimonials.tsx
```
Testimonials
├── Section
├── ImageWithFallback
├── Check (lucide-react)
├── useStaggeredAnimation
└── testimonials (content/testimonials.ts)
```

#### ContactForm.tsx
```
ContactForm
├── Section
├── CTAButton
├── useForm (カスタムフック)
├── validateContactForm
├── scheduleOptions (content/seminar.ts)
└── Formspree API (外部)
```

### カスタムフック

#### useForm
- **役割:** フォーム状態管理・バリデーション・送信
- **戻り値:** `{ form, errors, status, handleChange, handleSubmit }`

#### useStaggeredAnimation
- **役割:** 段階的アニメーション遅延を計算
- **戻り値:** `React.CSSProperties`（style属性用）

#### useScrollListener
- **役割:** スクロールイベントをthrottle（16ms間隔）
- **用途:** `StickyBottomCTA`の表示/非表示制御

---

## 6. 運用・開発ルール (Development Rules)

### 画像の配置

#### 配置場所
- **すべての画像:** `public/`ディレクトリ直下
- **推奨形式:** WebP（`.webp`）
- **命名規則:** ケバブケース（例: `koushi-jon-photo.webp`）

#### 画像一覧
```
public/
├── ashinara-houkan-daihyo.webp    # 受講生の声（中村様）
├── benefit1.webp ~ benefit5.webp  # 5つのベネフィット画像
├── eme-houkan-daihyo.webp         # 受講生の声（北山様）
├── first-houkan-daihyo.webp       # 受講生の声（高橋様）
├── Gemini_Generated_Image_*.webp   # Hero背景画像
├── kigyo-kanshu-shuryosho.webp    # 修了証書
├── kitayama-thumbnail.webp         # 動画サムネイル
├── koushi-jon-photo.webp          # 講師写真
├── nakamura-thumbnail.webp        # 動画サムネイル
├── nayami.webp                     # Problems背景画像
├── present.webp                    # CTA特典画像
└── takahashi-thumbnail.webp        # 動画サムネイル
```

#### 画像最適化ルール
1. **WebP形式を優先:** すべての画像は`.webp`に変換済み
2. **遅延読み込み:** `ImageWithFallback`コンポーネントで`loading="lazy"`をデフォルト適用
3. **優先読み込み:** ファーストビュー画像は`priority={true}`で`loading="eager"`
4. **非同期デコード:** `decoding="async"`でメインスレッドのブロックを防止

### パフォーマンス最適化

#### コード分割
- **Hero:** 通常ロード（ファーストビュー）
- **その他:** `lazy()` + `Suspense`で遅延読み込み

#### バンドル最適化
```javascript
// vite.config.js
manualChunks: {
  vendor: ["react", "react-dom"],
  icons: ["lucide-react"],
}
```

#### アニメーション
- **throttle間隔:** 16ms（60fps）
- **Intersection Observer:** `rootMargin: "50px"`, `threshold: 0.1`

### 状態管理

#### フォーム状態
- **カスタムフック:** `useForm`で一元管理
- **バリデーション:** `validation.ts`で定義
- **送信先:** Formspree API（環境変数不要）

#### グローバル状態
- **使用していない:** Redux/Zustand等の状態管理ライブラリは不使用
- **ローカル状態:** `useState`のみで管理

### アニメーション戦略

#### 段階的アニメーション
- **定数化:** `ANIMATION_DELAYS`で遅延時間を管理
- **計算:** `useStaggeredAnimation`で自動計算
- **適用:** `style`属性で`animationDelay`を設定

### 開発コマンド

```bash
# 開発サーバー起動
npm run dev

# 本番ビルド
npm run build

# リンター実行
npm run lint

# プレビュー（ビルド後）
npm run preview
```

### 環境変数

現在、環境変数は使用していません。  
将来的にFormspreeのエンドポイントを変更する場合は、`VITE_FORMSPREE_ENDPOINT`を追加可能。

### デプロイ

- **ホスティング:** Vercel推奨
- **ビルドコマンド:** `npm run build`
- **出力ディレクトリ:** `dist/`
- **Node.jsバージョン:** 18以上推奨

### コーディング規約

#### TypeScript
- **strict mode:** 有効
- **型定義:** `any`型は使用禁止
- **型ファイル:** `src/shared/types/index.ts`に集約

#### React
- **関数コンポーネント:** すべて関数コンポーネント
- **メモ化:** パフォーマンス最適化のため`React.memo`を積極的に使用
- **カスタムフック:** `use`プレフィックスで命名

#### CSS/Tailwind
- **クラス名:** Tailwindのユーティリティクラスを優先
- **カスタムクラス:** `index.css`で定義
- **クラス結合:** `cn()`ユーティリティを使用

### 重要な注意事項

1. **デザイン変更禁止:** ユーザーからの明示的な指示がない限り、デザイン・レイアウト・テキストは変更しない
2. **画像パス:** すべて`.webp`形式。`.png`や`.jpg`への変更は不可
3. **コンテンツ更新:** `src/content/*.ts`を編集することで、文言を変更可能
4. **コンポーネント追加:** 新しいセクションは`features/`に追加し、`App.tsx`で`lazy()`読み込み

---

## 📝 補足情報

### 未使用コンポーネント
以下のコンポーネントは削除済み（参考情報）:
- `src/features/ai-advisor/AiAdvisor.tsx`
- `src/features/achievements/Achievements.tsx`
- `src/shared/layout/ScrollToTopCTA.tsx`
- `src/shared/layout/FloatingCTA.tsx`

### 現在未使用のデータ
- `src/content/achievements.ts` - 実績データ（削除検討）
- `src/content/program.ts` - プログラム詳細（将来使用予定）

### 外部API連携
- **Formspree:** お問い合わせフォーム送信
- **YouTube:** 受講生インタビュー動画の埋め込み

---

**このドキュメントは、プロジェクトの全容を理解するための包括的なガイドです。**  
**質問や不明点があれば、このドキュメントを参照してください。**

